package layout

templ SectionHeader(title string, class string) {
	<h3 class={ "bg-base-200 w-full p-4 mb-0.5 flex items-center font-semibold justify-between gap-10 text-center text-2xl   " + class }>
		{ title }
	</h3>
}
